# Python
__pycache__/
*.pyc

# Local test recordings
recording_*.mp4

# Calibration: keep the per-camera JSON results, ignore captured frames and
# generated images (regenerate the board with `calibrate_camera.py make-board`)
receiver/calibration/captures/
receiver/calibration/checkerboard.png
receiver/calibration/*_verify.png

# ML model weights (auto-downloaded on first run) and demo snapshots
*.pt
*.pth
*.onnx
receiver/pose_snapshot.png
